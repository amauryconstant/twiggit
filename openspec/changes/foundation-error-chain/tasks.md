# Tasks

## 1. Setup

- [x] 1.1 Capture pre-change baseline by running `mise run test` and recording that the suite is green before any code edits
- [x] 1.2 Fix the `openspec/config.yaml` parse error at lines 134-140 by moving the `# Purity principles...` and `# Each rule is identified by...` comment lines out of the `specs:` list item to immediately above it (or removing them), then verify by running `openspec list --json` and confirming no `Warning: could not parse` line is printed
- [x] 1.3 Delete the empty stranded `openspec/changes/spec-restructure-all-categories/` folder with `git rm -r` and verify by running `ls openspec/changes/` and confirming only `foundation-error-chain/`, `archive/`, and `README.md` remain
- [x] 1.4 Verify `cmd/root.go` sets `SilenceUsage: true` and `SilenceErrors: true` on the root command (per `golang-spf13-cobra` best practice); if not, add them and verify by `grep -F 'SilenceUsage: true' cmd/root.go` returning exactly one match. Required so the cmd layer can format errors at the boundary without the auto-usage wall interfering with `IsCobraArgumentError` typed detection.

## 2. Domain layer: sentinels and first slice

- [x] 2.1 Create `internal/domain/sentinels.go` with the twelve `errors.New(...)` sentinel variables listed in the `domain-typed-errors` spec's Sentinel catalog requirement, verifying via `go build ./internal/domain/...` that the file compiles
- [x] 2.2 Refactor `WorktreeServiceError` end-to-end: rename `Cause` field to `Err`, update `Unwrap()` body to return `e.Err`, add `Is(target error) bool` returning `target == domain.ErrWorktreeNotFound`, delete the `IsNotFound() bool` method and its `strings.Contains` body; verify by `gopls rename` reporting zero unresolved references and `go vet ./internal/domain/...` clean
- [x] 2.3 Apply the same `Cause`→`Err` rename + `Is(target) bool` addition to `ProjectServiceError` (matching `ErrProjectNotFound`), `NavigationServiceError` (matching `ErrResolutionNotFound`), and `ResolutionError` (matching `ErrResolutionNotFound`); verify by `go build ./...` reporting zero compile errors
- [x] 2.4 Apply the same rename + `Is(target) bool` to `GitRepositoryError` (matching `ErrGitRepoNotFound`) and `GitWorktreeError` (matching `ErrWorktreeNotFound`); verify by `go build ./...` clean
- [x] 2.5 Add `func (e *ValidationError) Unwrap() error { return nil }`, drop the `💡` glyph from `ValidationError.Error()` so the message never contains a `💡` rune AND so the rendered message carries no trailing punctuation (no `.`, `!`, `?`, etc.) per the `golang-naming` error-string rule; verify by `grep -c '💡' internal/domain/service_errors.go` returning `0` and by `grep -nE 'Error\(\) string \{[^}]*\.$' internal/domain/service_errors.go` returning no matches
- [x] 2.6 Rewrite `internal/domain/shell_errors.go`: introduce a private `shellErrorBase struct { ShellType, Context, Err }`, drop the `Code string` field and the `ErrConfigFileNotWritable` constant, define seven concrete subtypes (`ShellAlreadyInstalledError`, `ShellNotInstalledError`, `ShellInvalidTypeError`, `ShellInferenceError`, `ShellDetectionError`, `ShellWrapperError`, `ShellConfigError`) each embedding the base struct and implementing `Is(target error) bool` matching its sentinel plus `Unwrap() error` returning the `Err` field; verify by `go build ./internal/domain/...` clean and by running `openspec validate foundation-error-chain --json` reporting `valid: true`

## 3. Service layer

- [x] 3.1 Update `internal/service/shell_service.go`: replace the two `errors.As(err, &shellErr) && shellErr.Code == ErrShellAlreadyInstalled/ErrShellNotInstalled` checks with `errors.Is(err, domain.ErrShellAlreadyInstalled)` / `errors.Is(err, domain.ErrShellNotInstalled)`, and replace `domain.NewShellError(domain.ErrInvalidShellType, ...)` constructors with the per-subtype constructor `domain.NewShellInvalidTypeError(...)`; verify by `go build ./internal/service/...` clean and `go vet ./...` clean
- [x] 3.2 Run `mise run lint:fix` and verify zero diffs remain in non-import order lint output

## 4. cmd layer

- [x] 4.1 Reduce CLI exit-code dispatch to three codes in `cmd/error_handler.go`: drop the `ExitCodeConfig`, `ExitCodeGit`, `ExitCodeValidation`, `ExitCodeNotFound` constants; rewrite `GetExitCodeForError` to dispatch `errors.As(err, &*domain.UsageError{})` (or `errors.Is(err, domain.ErrUsageFlag)`) → `ExitCodeUsage` (2), default `ExitCodeError` (1), nil → `ExitCodeSuccess` (0); remove the substring fallback (`strings.Contains(errStr, "invalid")`); verify by `go build ./cmd/...` clean
- [x] 4.2 Introduce `internal/domain/usage_error.go` with the `UsageError` type so the cmd dispatch can stay one line. Update `cmd/root.go`'s `SetFlagErrorFunc` to wrap cobra/pflag flag-parse errors via `domain.UsageWrap(err)`. Replace `IsCobraArgumentError` (and the type-preserved alias `IsCobraUsageError`) in `cmd/error_handler.go` with a single `errors.As(err, &*domain.UsageError{})` check; the seven-substring list and `cmd.ErrFlagUsage` sentinel are deleted. Update `cmd/init.go`'s `fmt.Errorf("%w: ...", ErrFlagUsage)` constructs to `domain.NewUsageError(message, nil)`. Arg-shape failures (added in task 4.8 via `cobra.Args:` validators) are blocked before `RunE` runs, so cobra's own `*cobra.FlagError` and `cobra.ErrSubCommandRequired` never reach `IsCobraUsageError` and remain unreachable. Verify by `go build ./cmd/...` clean, `go vet ./cmd/...` clean, and a manual run of `twiggit create` with no args producing exit code 2 via cobra's own usage-error path
- [x] 4.3 Add the private `asType[T error](err error) (T, bool)` generic helper to `cmd/error_formatter.go`, replacing the four `func() *T { target := &T{}; _ = errors.As(err, &target); return target }()` IIFE blocks in `formatValidationError`, `formatWorktreeError`, `formatProjectError`, and `formatServiceError` with `target, ok := asType[*domain.T](err); if !ok { return "" }; ...`; verify by `go build ./cmd/...` clean
- [x] 4.4 Register formatters in `NewErrorFormatterWithOptions`. The thirteen core formatters below SHALL be registered in this exact order (specific-before-generic per the `cli-error-formatting` spec's Type-matched dispatch requirement): `ShellAlreadyInstalledError`, `ShellNotInstalledError`, `ShellInvalidTypeError`, `ShellInferenceError`, `ShellDetectionError`, `ShellWrapperError`, `ShellConfigError`, `GitRepositoryError`, `GitWorktreeError`, `GitCommandError`, `NavigationServiceError`, `ResolutionError`, `ConflictError`. Service-wrapper formatters (`WorktreeServiceError`, `ProjectServiceError`, `ValidationError`, `ServiceError`) are appended after the thirteen in specific-before-generic order per the `cmd/AGENTS.md` recipe (Validation checked after service wrappers so a chain that wraps a `ValidationError` inside a service wrapper still renders through the outer wrapper's formatter). Verify by `go build ./cmd/...` clean and by `grep -nE 'RegisterFormatter' cmd/error_formatter.go` showing the thirteen core matches in the order above
- [x] 4.5 Implement the per-resource hint table in `cmd/error_formatter.go`: replace the literal `"Use 'twiggit list' to see available worktrees"` (and the project/worktree split elsewhere in the registry) with a `switch` over `errors.Is(err, sentinel)` returning the four hints from the `cli-error-formatting` spec's Actionable hints requirement; verify by `go build ./cmd/...` clean
- [x] 4.6 Update the worktree-deletion handler in `cmd/delete.go`: replace the three `errors.As + IsNotFound()` checks with a single `if errors.Is(err, domain.ErrWorktreeNotFound) { ... }`; verify by `go build ./cmd/...` clean
- [x] 4.7 Run `go test ./cmd/...` and verify the package compiles and pre-existing tests still pass before the rewrite in task 7
- [x] 4.8 Audit every `cmd/*.go` command for a `cobra.Args:` validator matching its actual arg shape (`cobra.ExactArgs(n)`, `cobra.MinimumNArgs(n)`, `cobra.MaximumNArgs(n)`, `cobra.RangeArgs(min, max)`, or `cobra.MatchAll(...)`); add the appropriate validator to each command that lacks one. Verify by `go build ./cmd/...` clean and `grep -nE 'Args:\s*cobra\.' cmd/*.go` returning one match per command

## 5. test/mocks layer

- [x] 5.1 Create `test/mocks/helpers.go` with `func variadicArgs(first any, opts ...any) []any { args := []any{first}; return append(args, opts...) }`; verify by `go build ./test/mocks/...` clean and `go vet ./test/mocks/...` clean
- [x] 5.2 Fix `MockContextService.GetCompletionSuggestions` and `MockContextService.GetCompletionSuggestionsFromContext` in `test/mocks/cmd_mocks.go`: replace `args := m.Called(partial, opts)` (and the three-arg variant) with `args := m.Called(variadicArgs(partial, opts...)...)`; verify by `go build ./test/mocks/...` clean
- [x] 5.3 Run `mise run lint:fix` and verify zero diffs remain in the mocks package

## 6. AGENTS.md updates

- [x] 6.1 Update `internal/service/AGENTS.md` to replace the example that uses `worktreeErr.Message == "worktree not found in any project"` with one showing `if errors.Is(err, domain.ErrWorktreeNotFound) { ... }`; verify by `grep -F 'Message ==' internal/service/AGENTS.md` returning no matches
- [x] 6.2 Update `cmd/AGENTS.md` to replace the IIFE recipe in the "Adding a new error type" section with one describing the `asType[T error](err) (T, bool)` helper; verify by `grep -F 'func() *' cmd/AGENTS.md` returning no matches
- [x] 6.3 Update the exit-code table in `cmd/AGENTS.md` to a 3-code mapping (drop rows for `ExitCodeConfig` (3), `ExitCodeGit` (4), `ExitCodeValidation` (5), `ExitCodeNotFound` (6)); verify by `grep -c 'ExitCode\(Config\|Git\|Validation\|NotFound\)' cmd/AGENTS.md` returning `0`
- [x] 6.4 Update the troubleshooting table in root `AGENTS.md` to a 3-code mapping (drop rows for codes 3-6); verify by `grep -E 'Exit code [3-6]' AGENTS.md` returning no matches. Add a one-line note that scripts keyed on the historical codes must update.

## 7. Tests (per project rule: written AFTER implementation)

- [x] 7.1 Rewrite `internal/domain/errors_test.go`: drop the `TestGitRepositoryError_IsNotFound` and `TestGitWorktreeError_IsNotFound` cases, add `TestErrSentinels_WalkThroughWraps` (table-driven: each NotFound sentinel wrapped through each corresponding error type returns true via `errors.Is`, each non-matching sentinel returns false); verify by `go test ./internal/domain/...` passing
- [x] 7.2 Rewrite `internal/domain/service_errors_test.go`: drop `TestWorktreeServiceError_IsNotFound`, add table-driven tests covering `errors.Is(err, ErrWorktreeNotFound)`, `errors.Is(err, ErrProjectNotFound)`, `errors.Is(err, ErrResolutionNotFound)` walks for `WorktreeServiceError`, `ProjectServiceError`, `NavigationServiceError`, `ResolutionError`; verify by `go test ./internal/domain/...` passing
- [x] 7.3 Rewrite `internal/domain/shell_errors_test.go`: assert each subtype's `Is(target)` returns true only for its own sentinel, each subtype's `Unwrap()` returns its `Err` field, and the Sentinel catalog identifiers and messages in `internal/domain/sentinels.go` exactly match the spec table; verify by `go test ./internal/domain/...` passing
- [x] 7.4 Rewrite `cmd/error_handler_test.go`: drop the substring-based tests AND all per-exit-code assertions for codes 3-6. Add `TestGetExitCodeForError_ThreeCodeDispatch` (table-driven: each error class — `ValidationError`, `GitRepositoryError`, `WorktreeServiceError`, generic `errors.New("...")` — collapses to `ExitCodeError` (1); nil returns `ExitCodeSuccess` (0); any `*domain.UsageError` (with or without a wrapped cause) returns `ExitCodeUsage` (2)). Add `TestCategorizeError_SentinelNotFoundDispatch` (each of the four NotFound sentinels produces `ErrorCategoryNotFound`); add `TestCategorizeError_UsageError` (a `*domain.UsageError` returns `ErrorCategoryCobra`). Verify by `go test ./cmd/...` passing
- [x] 7.5 Rewrite `cmd/error_formatter_test.go`: drop the IIFE tests, add per-formatter registration coverage including the seven new shell subtypes plus `TestFormatter_NotFoundHintDiscrimination` (the four NotFound sentinels each emit their per-resource hint per the `cli-error-formatting` Actionable hints requirement); verify by `go test ./cmd/...` passing
- [x] 7.6 Update `test/e2e/list_test.go` lines 180 and 199: replace `ShouldFailWithExit(session, 5)` with `ShouldFailWithExit(session, 1)` (validation errors now exit 1, not 5). Verify by `go test ./test/e2e/...` passing
- [x] 7.7 Run `mise run test` and verify the full suite is green and that the new sentinel-walk assertions across `internal/domain` and `cmd` confirm both `errors.Is(err, ErrXNotFound)` and `errors.As(err, &typedErr)` returns

## 8. Verification and release

- [x] 8.1 Run `mise run verify` and verify lint, format, race, and golden-file tests all pass
- [x] 8.2 Introduce `internal/domain/usage_error.go` with the `UsageError` type (carrying `Message string` and `Err error`, plus `Unwrap() error` and `Is(target error) bool` matching `domain.ErrUsageFlag`) so cobra/pflag flag-parse errors and cmd-side usage failures share one domain contract. Add `ErrUsageFlag = errors.New("domain: usage flag error")` to the sentinel catalog. Replace the typed pflag walks (`*pflag.ValueRequiredError`, `*pflag.InvalidValueError`, `*pflag.InvalidSyntaxError`) and `cmd.ErrFlagUsage` in `cmd/error_handler.go` with a single `errors.As(err, &*domain.UsageError{})` walk. Wrap cobra/pflag flag-parse errors in `domain.UsageWrap(err)` from `cmd/root.go`'s `SetFlagErrorFunc`. Replace `cmd/init.go`'s `fmt.Errorf("%w: ...", ErrFlagUsage)` constructs with `domain.NewUsageError(...)`. Amend `domain-typed-errors` and `cli-error-formatting` specs to enumerate `*domain.UsageError` and `domain.ErrUsageFlag` as the typed usage-error sentinels. The spec-listed `*cobra.FlagError` / `cobra.ErrSubCommandRequired` are blocked earlier by `cobra.Args:` validators before `RunE` runs and remain documented as unreachable. Verify by `openspec validate foundation-error-chain --json` reporting `valid: true` and `go test ./cmd/... ./internal/domain/...` passing.
- [x] 8.3 Strike the 0.13.0 version-bump claim from `proposal.md` and `design.md`; bump is deferred to the next change that surfaces a release-pinned tag. Verify by `grep -E '0\.13\.0' openspec/changes/foundation-error-chain/proposal.md openspec/changes/foundation-error-chain/design.md` returning 0 matches.
- [x] 8.4 Migrate `MockContextService.GetCompletionSuggestionsFromContext` in `test/mocks/cmd_mocks.go:233` to use the `variadicArgs(... )...` expansion pattern matching `GetCompletionSuggestions` at line 224, so both methods expand variadic `opts ...domain.SuggestionOption` before reaching `m.Called`. Verify by `go test ./test/mocks/...` and the impacted e2e suite remaining green.
- [x] 8.5 Restructure `cli-error-formatting/spec.md` so the existing `Exit code mapping` requirement moves out of `## MODIFIED Requirements` into a `## REMOVED Requirements` block (verbatim, including the historic 5/4/6-titled scenarios and the obsolete `ExitCodeConfig` / `ExitCodeGit` / `ExitCodeValidation` / `ExitCodeNotFound` rows) and a fresh `Exit code mapping` requirement is added under `## ADDED Requirements` with 3-code-only scenario titles (`ExitCodeError`, `ExitCodeUsage`, `ExitCodeSuccess`) and no historical exit-code references, per the `golang-cli-architecture` skill's 3-code contract. Verify by `openspec validate foundation-error-chain --json` reporting `valid: true` and by `awk '/^## (MODIFIED|ADDED) Requirements/{section=$0; next} /^## REMOVED Requirements/{section=""; next} section && /exit [3-6]|ExitCode(Validation|Git|NotFound|Config)/{print FILENAME":"NR":"$0}' openspec/changes/foundation-error-chain/specs/cli-error-formatting/spec.md` returning 0 matches.
