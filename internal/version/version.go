package version

var (
	// Version is the current version of twiggit (injected at build time)
	Version = "dev"
	// Commit is the git commit hash (injected at build time)
	Commit = ""
	// Date is the build date (injected at build time)
	Date = ""
)
